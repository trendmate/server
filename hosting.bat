git checkout main
git pull 
git checkout hosting
git checkout main -- config
git checkout main -- models
git checkout main -- scrapers
git checkout main -- server
git checkout main -- utils
git checkout main -- .gitignore
git checkout main -- app.py
git checkout main -- .Procfile
git checkout main -- requirements.txt
git checkout main -- runtime.txt
git checkout main -- server_requirements.txt
git checkout main -- server_setup.sh
git checkout main -- nginx.conf
git checkout main -- nginx.conf
git checkout main -- start.sh
git commit -m "v0.1"
git push